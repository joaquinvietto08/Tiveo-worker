import { StyleSheet } from "react-native";
import { colors } from "../../styles/globalStyles";

export const styles = StyleSheet.create({
  messages__mainContainer: {
    flex: 1,
    backgroundColor: colors.background,
  },
});
